<!DOCTYPE html>
<html lang="en">
    <head>
      <meta charset="UTF-8">
      <title>MY APP</title>
     <link rel="stylesheet" type="text/css" href="/css/style.css">
     <script src="/js/script.js"></script>
    </head>
    <body>
      <h1>May Node and Express be with you.</h1>
      <form action="/quotes" method="POST">
        <input type="text" placeholder="Enter character name" name="name" required="required" pattern=".+[a-zA-Z\d]">
        <input type="text" placeholder="Enter dialogues" name="quote" required="required">
        <button type="submit">Submit</button>
      </form>
      <div>
       <input type="checkbox" class="j-sel-quo-all" onclick="toggleAllCheckbox(this);" /> Select All
       </div>
      <div class="cr-quote-div">
        <% for(var i=0; i<quotes.length; i++) {%>
          <div class="quote" style="width: 100%;">
            <input type="checkbox" class="j-sel-quo" objid="<%= quotes[i]._id %>"/>
            <span class="j-del-quo cr-del-quo">
              <i class="fa fa-trash-o" aria-hidden="true"></i>
            </span>
            <b><%= quotes[i].name %></b> : <span>" <%= quotes[i].quote %> "</span>
          </div>
        <% } %>
      </div>
      
	</body>
</html>